<!-- Binding repo-scale exploration and scoped re-exploration calls only (references/binding-protocol.md). Replaces role-and-rules.md for these calls — they are not rounds: no verdict line, no cap, no ledger. Slots: {{SHA}} — the commit being explored (the base SHA, or the current revision `Sk` for a scoped re-exploration); {{CHARGE}} — a bounded exploration charge (map a subsystem, trace a boundary, verify a property — never just "look around"). -->
You are exploring the tree at {{SHA}}. Read-only commands are permitted; modifying files remains forbidden; reply with your findings as text.
Your charge, scoped to tracked content (the SHA pins nothing else): {{CHARGE}}
Cite `file:line` for every claim. Do not include a VERDICT line: this is exploration, not a debate round.
