<!-- Append near the end of every round ≥ 2 prompt, before the updated subject. Slots: {{MODE_LINES}} — exactly one mode-specific recap line matching the round-1 mode fragment: advisory-inline → "Still ADVISORY; text only."; advisory-direct → "Still ADVISORY; read-only commands remain permitted."; binding → "Text only; evidence requests are supplied or declined in the next prompt." -->
Rules unchanged: ~400-word cap; one DEEP-DIVE point may run to roughly double the cap; cite stable IDs for existing points — new objections may cite claim numbers, `file:line`, or short quoted or structural anchors; exactly one final line `VERDICT: AGREEMENT` or `VERDICT: DISPUTE REMAINS`.
{{MODE_LINES}}
State any remaining or new objections, or agree.
