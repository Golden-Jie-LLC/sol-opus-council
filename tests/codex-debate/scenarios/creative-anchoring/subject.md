# The Recursion Limerick

There once was a coder named Lee
whose functions were nested more than three levels deep;
    each callback would call
    yet another, and all
returned undefined — eventually.
